[user]
	name = Chris Hedley
	email = chris@cghsystems.net
	initials = cgh
[core]
	excludesfile = ~/.gitignore
	autocrlf = input
[color]
	ui = true
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 
[push]
	default = simple
[alias]
  st = status
	br = branch
	ci = commit
	co = checkout
	conflict = diff --name-only --diff-filter=U
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	show-git-files = ls-tree -r master --name-only
	r = pull --rebase
	p = push
	pci = pair-commit
[credential] 
  helper = cache
[url "git@github.com:"]
	insteadOf = https://github.com/
[init]
	defaultBranch = main
